syntax = "proto3";

package automation;

option go_package = "/;automation";

message TestRequest {
    string name = 1;
    int32 id = 2;
}

message TestResponse {
    string message = 1;
    bool success = 2;
}

service TestService {
    rpc TestMethod (TestRequest) returns (TestResponse);
}