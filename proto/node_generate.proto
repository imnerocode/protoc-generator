syntax = "proto3";

package automation;


message TestRequestNode {
    string name_node = 1;
    int32 id_node = 2;
}

message TestResponseNode {
    string message_node = 1;
    bool success_node = 2;
}

service TestServiceNode {
    rpc TestMethodNode (TestRequestNode) returns (TestResponseNode);
}
